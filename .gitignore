# Never commit device credentials or the APK payload
.env
*.env
*.apk
*.xapk
*.ab
__pycache__/
*.pyc
# scratch key material
*local_key*
*.bmp

# Reverse-engineering notes + superseded/cloud-path tooling: local-only, not
# part of the published integration. See research/README.md.
/research/

# Owner's real setup values (real IPs, ids, account identifiers). Never commit.
/MY-SETUP.md

# Cloud-path credential notes (appKey/appSecret) live in research/, not the public tree.
/CREDENTIALS.md

# Owner's private research trail, planning notes, and key-extraction tooling.
# Not part of the published integration. Stays local, never committed.
/local/

# Claude Code session data, plus the nested full checkouts it creates under
# .claude/worktrees/. Transcripts and a second copy of the repo; never part of
# the published integration.
/.claude/
